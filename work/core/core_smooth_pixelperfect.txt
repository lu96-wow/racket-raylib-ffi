文件: core_smooth_pixelperfect.c
使用结构体:
  Camera2D          — src/raylib.h:338
  RenderTexture2D   — src/raylib.h:294 (typedef RenderTexture)
  RenderTexture     — src/raylib.h:288
  Texture           — src/raylib.h:273 (通过 target.texture)
  Rectangle         — src/raylib.h:256
  Vector2           — src/raylib.h:216

使用函数:
  InitWindow / SetTargetFPS / WindowShouldClose / CloseWindow
  GetFrameTime / GetTime
  IsKeyPressed(KEY_S / KEY_O)
  GetScreenWidth / GetScreenHeight
  LoadRenderTexture / UnloadRenderTexture
  BeginTextureMode / EndTextureMode
  BeginMode2D / EndMode2D
  ClearBackground
  DrawRectanglePro(Rectangle, Vector2, float, Color)
  DrawTexturePro(Texture, Rectangle, Rectangle, Vector2, float, Color)
  DrawText / DrawFPS
  sinf / cosf / truncf (Racket sin / cos / truncate)

新增绑定 (rshapes.rkt):
  draw-rectangle-pro  "DrawRectanglePro"  (_fun (r : _rect-bytes) (o : _vec2-bytes) _float (col : _color-bytes) -> _void)

复用绑定:
  load-render-texture / unload-render-texture (rtextures.rkt)
  begin-texture-mode / end-texture-mode (rtextures.rkt)
  draw-texture-pro (rtextures.rkt)
  begin-mode-2d / end-mode-2d (rcore.rkt)
  get-frame-time / get-time (rcore.rkt)
  get-screen-width / get-screen-height (rcore.rkt)
  is-key-pressed / draw-fps (rcore.rkt)
  camera2d / set-camera2d-target-x! / set-camera2d-target-y! / camera2d-target-x / camera2d-target-y (raylib-var)
  rectangle / set-rectangle-x!/y!/w!/h! (raylib-var)
  vector2 (raylib-var)
  RAYWHITE / LIGHTGRAY / DARKBLUE / DARKGREEN / RED / BLUE / BLACK / WHITE / LIME / GREEN

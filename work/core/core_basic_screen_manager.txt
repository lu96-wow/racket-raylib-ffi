文件: core_basic_screen_manager.c
使用结构体:
  (无) — 仅使用基本类型和颜色宏/枚举，未使用任何 raylib 结构体变量

使用函数:
  InitWindow / SetTargetFPS / WindowShouldClose / CloseWindow
  BeginDrawing / EndDrawing / ClearBackground
  DrawText / DrawRectangle
  IsKeyPressed(KEY_ENTER)
  IsGestureDetected(GESTURE_TAP)

新增绑定:
  (无需新增 — 所有函数/常量均已绑定)

复用绑定:
  init-window / close-window / window-should-close? / set-target-fps
  begin-drawing / end-drawing / clear-background
  draw-text / draw-rectangle
  is-key-pressed (KEY-ENTER)
  is-gesture-detected? (GESTURE-TAP)
  RAYWHITE / LIGHTGRAY / GRAY / GREEN / DARKGREEN
  PURPLE / MAROON / BLUE / DARKBLUE

文件: core_custom_frame_control.c
使用结构体:
  (无) — 仅使用基本类型和颜色宏，未使用任何 raylib 结构体变量

使用函数:
  GetTime() → double
  PollInputEvents()
  IsKeyPressed(KEY_SPACE / KEY_UP / KEY_DOWN)
  BeginDrawing / EndDrawing / ClearBackground
  DrawRectangle / DrawCircle / DrawText
  GetScreenWidth / GetScreenHeight
  SwapScreenBuffer()
  WaitTime(double seconds)
  TextFormat (用 Racket format 替代)

新增绑定 (rcore.rkt):
  get-time           "GetTime"           (_fun -> _double)
  swap-screen-buffer "SwapScreenBuffer"  (_fun -> _void)
  wait-time          "WaitTime"          (_fun _double -> _void)

复用绑定:
  poll-input-events / is-key-pressed
  begin-drawing / end-drawing / clear-background
  draw-rectangle / draw-circle / draw-text
  get-screen-width / get-screen-height
  RAYWHITE / SKYBLUE / RED / MAROON / BLACK / DARKGRAY / GRAY / LIME / GREEN

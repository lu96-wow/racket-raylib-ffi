file: core_automation_events.c
status: 已实现 — 纯 Racket 录制 + 单 FFI 回放

绑定方案:
  raylib/rcore.rkt:
    _automation-event-bytes   — AutomationEvent 传值类型 (24B, _list-struct)
    play-automation-event     — 原始 FFI, 接受 (list frame type p0 p1 p2 p3)

  raylib-racket/automation.rkt （纯 Racket 兼容实现层）:
    automation-event struct   — 事件结构体 {frame, type, params}
    play-automation-event     — 对 rcore.rkt 原始 FFI 的结构体包装
    export-automation-events  — 纯 Racket 导出 .rae 文件
    load-automation-events    — 纯 Racket 加载 .rae 文件
    recorder / make-recorder  — 帧间状态追踪
    record-frame!             — 每帧录制输入变化
    EVENT-NONE ~ ACTION-SETTARGETFPS  — 23 个事件类型常量

设计说明:
  C 的 RecordAutomationEvent() 录制逻辑完全由纯 Racket 替代:
    - 每帧通过已有 FFI (is-key-down, get-mouse-position 等) 查询输入状态
    - 对比上一帧状态, 差异构造成 automation-event 追加到列表
    - 按 S 停止时, reverse 列表得到正确的时序, 导出 .rae 文件
    - 回放时遍历 events[i], 调用 play-automation-event 传给 C
  FFI 只有 PlayAutomationEvent 一个函数 (传值, 24B 压栈, 无指针问题)
  不再有 SetAutomationEventList / 'raw malloc / C 持指针等复杂机制

使用方式:
  (require "../../raylib/raylib.rkt"           ;; FFI 绑定 + 常量
           "../../raylib-racket/automation.rkt")  ;; 纯 Racket 自动化层
